# Subcontinental Planet
part = {
    location = 0
    localization = "START_SCREEN_PART_ONE_SUBCONTINENTAL"

    trigger = {
        ideal_planet_class = pc_subcontinental
        NOT = { has_country_flag = custom_start_screen }
    }
}